# Публичная ссылка без карты и без облака (через localtunnel).
# Сначала в другом окне: node server.js
Write-Host "Туннель к http://localhost:3000 (нужен интернет)..."
npx --yes localtunnel --port 3000
